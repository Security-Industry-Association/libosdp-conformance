acurxsize - sends the appropriate value (OSDP_BUF_MAX) in an osdp_ACURXSIZE
message.

factory-default - resets parameters to factory default, which
at the moment means delete the saved parameter file.

